# Self-Contained Local Testing Environment
# Copy this to .env.local for completely local development
# Usage: docker-compose -f docker-compose.local.yml up --build

# Local Testing Mode (bypasses Cognito)
BYPASS_COGNITO_AUTH=true
LOCAL_TESTING_MODE=true

# Model Service (external - works fine)
ACTIVE_MODELS_URL=https://active.dev.mor.org/active_models.json
DEFAULT_FALLBACK_MODEL=mistral-31-24b

# Proxy Router (can use dev environment or mock locally)
PROXY_ROUTER_URL=http://router.dev.mor.org:8082

# Local Development Settings
LOG_LEVEL=DEBUG
ENVIRONMENT=local

# Optional: Mock proxy router for completely offline testing
# PROXY_ROUTER_URL=http://mock-router:8082
# MOCK_PROXY_ROUTER=true
